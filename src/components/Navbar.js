import { Menu, MenuItem } from "semantic-ui-react";

const Navbar = () => (
    <Menu
        secondary
        size="large"
        fixed="top"
    >
        <MenuItem>
        </MenuItem>
    </Menu>
)